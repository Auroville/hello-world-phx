tasks:
  - name: Install front-end assets
  - before: mix deps.get
    init: psql --command "CREATE USER postgres WITH SUPERUSER PASSWORD 'postgres';" |
      cd /workspace/hello-world-phx |
      mix ecto.create
    command: mix ecto.migrate && mix run priv/repo/seeds.exs && iex -S mix phx.server   

ports:
  - port: 4000
    onOpen: open-browser
